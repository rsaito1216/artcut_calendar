<div class="contents row">
  <div class="container">
<h2>パスワードを忘れた場合、下記の欄に登録したメールアドレスを入力してください。パスワードをリセットするために、メールアドレスに確認のメールを送信します。</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="actions">
    <%= f.submit "パスワードをリセットする" ,data: { confirm: "登録したメールアドレスで間違いなく入力出来ていますか?" } %>
  </div>
<% end %>
  </div>
</div>
