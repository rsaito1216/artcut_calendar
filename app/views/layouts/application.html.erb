<!DOCTYPE html>
<html>
  <head>
    <title>ArtCalendar</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><a href= / >アート理容室予約カレンダー</a></h1>
        <% if user_signed_in? %>
          <div class="user_nav grid-6">
            <span><%= current_user.nickname %>さん
            <ul class="user__info">
              <li>
                <%= link_to "予約表示", "/users/#{current_user.id}" %>
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
                <li><%= link_to '情報編集', edit_user_registration_path, method: :get %></li>
              </li>
            </ul>
          </span>
            <%= link_to '予約新規登録', new_event_path, class: "post" %>
          </div>
        <% else %>
          <div class="grid-6">
            <%= link_to "ログイン", new_user_session_path, class: "post" %>
            <%= link_to "ユーザー登録", new_user_registration_path, class: "post" %>
          </div>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer>
      <font color="brack"><p>
        営業時間AM9時〜PM7時 毎週月曜、第二第三火曜定休日(その他年始年末休みなどカレンダーに表示されています)
      </p></font>
      <p>
        Copyright アート理容室 2020.
      </p>
    </footer>
  </body>
</html>
